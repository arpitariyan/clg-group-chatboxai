// This code has been moved to LayoutContent.jsx
// All functionality is now handled in the main layout header

/*
import { Button } from '@/components/ui/button'
import { Clock, Link, Send } from 'lucide-react'
import moment from 'moment'
import React from 'react'

function Header({ searchInputRecord }) {

    return (
        <div className='flex items-center justify-between'>
            <div className='flex items-center gap-2 text-gray-500'>
                <Clock className='w-4 h-4' />
                <h2 style={{ fontSize: '80%' }}>{moment(searchInputRecord?.created_at).fromNow()}</h2>
            </div>

            <h2 className='line-clamp-1 max-w-md text-sm text-gray-500'>{searchInputRecord?.searchInput}</h2>

            <div className='flex items-center gap-2'>
                <Button className='cursor-pointer'><Link /></Button>
                <Button className='cursor-pointer'><Send />Share</Button>
            </div>
        </div>
    )
}

export default Header
*/
